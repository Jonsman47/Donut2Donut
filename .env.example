# Database (Postgres recommended). For quick local testing you can use SQLite:
# DATABASE_URL="file:./dev.db"
DATABASE_URL="postgresql://user:pass@localhost:5432/donutsmp_market"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change_me"

# Stripe (optional for dev — if missing, app uses DEV payment mode)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Optional: Discord auth (keep commented until you add keys)
# DISCORD_CLIENT_ID="..."
# DISCORD_CLIENT_SECRET="..."
